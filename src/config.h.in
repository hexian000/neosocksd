#ifndef CONFIG_H
#define CONFIG_H

#define PROJECT_NAME "@PROJECT_NAME@"
#define PROJECT_VER "@PROJECT_VERSION_STRING@"
#define PROJECT_HOMEPAGE "@PROJECT_HOMEPAGE_URL@"

#cmakedefine01 HAVE_IP_TRANSPARENT
#cmakedefine01 HAVE_SO_BINDTODEVICE
#cmakedefine01 HAVE_SO_REUSEPORT
#cmakedefine01 HAVE_TCP_FASTOPEN

#define WITH_TPROXY (HAVE_IP_TRANSPARENT && !POSIX)
#define WITH_NETDEVICE (HAVE_SO_BINDTODEVICE && !POSIX)
#define WITH_REUSEPORT (HAVE_SO_REUSEPORT && !POSIX)
#define WITH_FASTOPEN (HAVE_TCP_FASTOPEN && !POSIX)

#endif /* CONFIG_H */
